/* Offline art bundle part 2 — full base64 bundle (~471KB) not pushed via MCP due to size.
 * Game falls back to PokéAPI CDN / images/*.png when EMBEDDED_IMAGES is empty.
 */
window.EMBEDDED_IMAGES = Object.assign(window.EMBEDDED_IMAGES || {}, {});
